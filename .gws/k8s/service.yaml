apiVersion: v1
kind: Service
metadata:
  name: nodejs-hono
  namespace: nodejs-hono
  labels:
    app: nodejs-hono
spec:
  type: ClusterIP
  selector:
    app: nodejs-hono
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
